# Recordings

InmarScope saves received voice recordings here by default. Change the output
folder in the app's recording controls if desired. Your recordings remain local
and are not included in the project's commits or release packages.

The public upstream source repository contains no recordings folder or sample
recordings. This is a ready-to-use output folder, not a copied sample library.
